import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.munchimerchant.app',
  appName: 'munchi-merchant',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
